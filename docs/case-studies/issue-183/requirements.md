# Issue 183 requirement ledger

This ledger combines the body of
[issue 183](https://github.com/link-foundation/relative-meta-logic/issues/183),
the current [PR 184 description](https://github.com/link-foundation/relative-meta-logic/pull/184),
and every requirement-bearing review or maintainer comment through
2026-09-23T16:58:55Z (conversation comment `5799100000`). A delivered guard
is marked complete only when the repository contains both implementation and
automated evidence. `Partial`, `Open`, and `Blocked by upstream` are first-class
statuses and all prevent the separate final-completion gate from passing. A
research question remains open even when its boundary and non-claim are fully
enforced.

## Source register

The register distinguishes requirements from automated work-session notices,
solution logs, implementation summaries, and ready-to-merge notices. Those
status-only comments add no requirement. The PR review and inline-review
channels were also checked and contain no comments. Repeated requests to
double-check everything remain registered because they require the ledger and
its completeness test, even though they add no new semantic capability.

| Source | Requirement extracted | Atomic rows |
|--------|-----------------------|-------------|
| [Issue body](https://github.com/link-foundation/relative-meta-logic/issues/183) | Base RML on meta-theory; introduce set/type theory; provide several set encodings and cyclic link sequences; define meta-theory through sets, types, and itself; unify terms and addresses; test, document, and validate the complete result. | R1-R8, R11-R13, R19-R21 |
| [Current PR description](https://github.com/link-foundation/relative-meta-logic/pull/184) | Preserve the executable-source, provenance, metrics, reproduction, compatibility, verification, release, alternative-foundation, independent-symmetry, and observation-loss claims made by the deliverable itself. | R22-R44, R57-R95 |
| [Comment 5750752869](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5750752869) | Re-audit the issue and architecture at every level. | R21, R52, R56 |
| [Review 5750869373](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5750869373) | Resolve and execute definition witnesses; reject missing or corrupted proofs; distinguish declarations from verified definitions; implement actual set semantics and self-execution. | R2-R5, R24, R28, R45 |
| [Review 5750923882](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5750923882) | Model proof roles explicitly; validate allowed rules, premises, substitutions, dependencies, exact conclusions, and non-circular justification. | R28, R31, R45, R46 |
| [Comment 5750925476](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5750925476) | Use links-network terminology repository-wide; make graphs a subset; implement graph theory and relational algebra over linked set/type foundations. | R9, R10, R50, R52 |
| [Comment 5751189191](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5751189191) | Implement the named theories rather than only their declarations and verify every discussion requirement. | R19, R21, R47, R56 |
| [Review 5752324661](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5752324661) | Bind typed proofs to exact judgements; separate candidate input from its trusted basis; document the nested-set representation boundary. | R4-R6, R45, R46 |
| [Comment 5756803396](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5756803396) | Preserve all pinned definitions, recursive bodies, axioms, theorems, proofs, and dependencies; keep reasoning associative and eliminate hard-coded concepts as far as the executable boundary permits. | R14-R18, R23-R24, R47-R49, R55 |
| [Comment 5758516426](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5758516426) | Treat the preceding full-content requirement as mandatory rather than deferred. | R47-R49, R56 |
| [Review 5760183637](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5760183637) | Upgrade the 229-name inventory to complete syntax, signatures, bodies, proof source, recursion, dependencies, mutation checks, and declaration-level parity; remove theory-specific adapters. | R13-R15, R47, R48 |
| [Review 5761499497](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5761499497) | Move from linked storage to linked semantics; execute unknown binding/substitution/reduction systems without host callbacks or external kernels. | R15-R18, R24, R48, R49, R55 |
| [Comment 5763894325](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5763894325) | Make RML a links-based universal formal-system constructor; do not delegate reasoning to Lean/Rocq; support arbitrary relative foundations, rebinding, traditional and associative presentations, minimal axioms, full self-implementation, current dependencies, and repository-wide vision alignment. | R14-R18, R20-R26, R47-R55 |
| [Review 5764336389](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5764336389) | Expose a minimal meta-foundation; implement generic linked binding, matching, substitution, derivation, and verification; execute one theory over two foundations; enumerate and reduce host operations. | R23-R28, R53-R55 |
| [Review 5766128876](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5766128876) | Run the K0 minimization loop, strengthen self-interpretation, publish and negatively audit the trust graph, avoid irreducibility claims, and justify every residual primitive. | R27-R32 |
| [Comment 5766158556](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5766158556) | Reconcile the complete issue and conversation with full self-hosting evidence. | R21-R44, R56 |
| [Review 5766978427](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5766978427) | Measure total and independent host semantics, duplication, closure, layers, runtime trust coverage, compression, and previous/current deltas. | R33-R40 |
| [Comment 5767102441](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5767102441) | Continue until all discussion requirements are demonstrably complete. | R21, R56 |
| [Review 5768051551](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5768051551) | Use the metrics to reduce the external semantic surface to an experimentally justified fixed point rather than stopping at measurement. | R33-R40 |
| [Comment 5768059977](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5768059977) | Re-audit completeness after the quantitative review. | R21, R56 |
| [Review 5769567009](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5769567009) | Do not confuse universal combinators with a link-native foundation; remove the external host semantic description; classify every residual law's provenance; test a zero-transition candidate and equivalent re-encodings. | R41-R44 |
| [Comment 5769569663](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5769569663) | Continue until every requirement is complete after the provenance review. | R21, R56 |
| [Comment 5772097742](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5772097742) | Perform another whole-discussion completeness review. | R21, R56 |
| [Comment 5772805558](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5772805558) | Commit an exhaustive Markdown ledger of every issue and PR requirement and verify full implementation. | R21, R56 |
| [Review 5773411325](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5773411325) | Correct path dependence with multiple non-equivalent candidates, a common workload, architecture-neutral metrics, removal tests, executable equivalence only, and a machine-readable comparison without a global minimality claim. | R57-R61, R68-R69 |
| [Comment 5773514118](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5773514118) | Fully implement the alternative search; represent substitution and reasoning artefacts as links; provide self-hosting, multiple relative foundations, Turing-completeness evidence, executable language cores, referential-link research, parity, and exhaustive documentation. | R57-R69 |
| [Review 5775207546](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5775207546) | Do not rank one reduced/self-hosted candidate against privileged-host controls; require comparable reduction, separate universality from minimality, keep the search open without peers, and answer which authority is intrinsic to links. | R60-R61, R64-R65, R70-R72 |
| [Review 5776265940](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5776265940) | Do not confuse a host representation with link ontology, treat passivity/external transition and the structure/transformation split as assumptions, and do not infer that links have no intrinsic execution principle. | R71, R73 |
| [Review 5777314436](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5777314436) | Keep the foundational questions visibly open; preserve an independent, constraint-led search; audit every primitive's provenance; treat A/B/C as controls rather than ontology; and avoid using “link-native” to imply derivation from link ontology. | R71, R73-R76 |
| [Review 5778389052](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5778389052) | Keep PR metadata consistent with the explicitly open foundational investigation and do not auto-close issue 183. | R77 |
| [Review 5778625533](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5778625533) | Advance the foundational investigation with new falsifiable derivations, eliminations, or representation-dependence evidence instead of adding only unresolved-state metadata and guards. | R71, R73-R74, R78 |
| [Review 5780145503](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5780145503) | Measure what the binary observation forgets; separate intentional quotients from potentially fundamental losses; test distinctions across independent representations; derive rather than stipulate additions; preserve the destination-independent method; keep ontology and semantics open; and remove the stale auto-close directive. | R71, R73-R74, R77, R79-R87 |
| [Review 5781219145](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5781219145) | Separate asymmetry forced by the base observation from asymmetry supplied or enabled by the conditional refinement; count singleton orbits rather than only classes containing one; scope finite invariants to tested widths; and make live PR metadata verification fail when the external state is false. | R71, R73-R74, R77, R85, R88-R91 |
| [Review 5782983781](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5782983781) | Determine whether the possibility of interaction is forced by the base or supplied by the observation model; answer with an elimination, invariant, countermodel, unavoidable structure, or representation-dependence result rather than a new relation, semantic role, calculus, or audit layer. | R71, R73-R74, R92 |
| [Comment 5783034346](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5783034346) | Perform the foundational investigation directly in terms of links and their relabellings rather than using set, category, or type theory as foundational authority. | R73-R74, R93 |
| [Review 5784120161](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5784120161) | Audit whether the starting “unlabelled occurrences + coincidence” projection already forgets independently justified intrinsic information; provide an invariant, countermodel, no-go result, or unavoidable structure without simply adding a desired observable or importing a familiar foundation. Also remove the renewed issue-closing directive. | R71, R73-R74, R77, R94 |
| [Review 5785148374](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5785148374) | Audit the equivalences assumed by the repaired addressable representation: distinguish transformations justified by link observations from observer-introduced quotients, test whether a smaller faithful descriptor or additional intrinsic structure is forced, and keep the investigation free of imported evaluators, types, sets, categories, or calculi. | R71, R73-R74, R80, R95 |
| [Review 5786000580](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5786000580) | Apply the same provenance test to every remaining representation assumption and produce another falsifiable invariant, countermodel, unavoidable distinction, representational equivalence, no-go result, or eliminated assumption. | R71, R73-R74, R96, R122-R126 |
| [Requirements audit 5791257637](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5791257637) | Restore truthful exhaustive tracking; implement full four-language structured representations and translations, recursively linked default typing, multiple linked foundations, native meta-theory workflows, continued foundation research, dependency delivery, self-implementation, and enforceable completion. | R11, R18, R21, R47, R49, R51-R52, R55-R56, R62-R66, R77, R87, R97-R137 |
| [Comment 5796435750](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5796435750) | Classify self-incidence per reference slot. | R96, R138 |
| [Review 5799100000](https://github.com/link-foundation/relative-meta-logic/pull/184#issuecomment-5799100000) | Keep the slot masks uninterpreted; audit and vary remaining ordered-address assumptions one at a time; distinguish contract-forced, representation-stable, observer-added, and tempting semantic structure; produce another falsifiable result without selecting a destination. | R71, R73-R74, R86, R93, R96, R127, R139 |

The exact original wording is retained at the linked immutable comment IDs.
The **Requirement** column below is its normalized atomic form. The final
column records delivered implementation and positive evidence first, then the
specific negative evidence or remaining gap in backticks. This keeps a small
finite demonstration from silently replacing its broader parent obligation.

## Atomic requirement ledger

| ID | Requirement | Status | Implementation and evidence |
|----|-------------|--------|-----------------------------|
| R1 | Base RML on Links Theory/meta-theory. | Complete | `rml-by-links` in `core.lino`; mirrored definition-chain tests. |
| R2 | Define Links Theory through set theory and type theory. | Complete | `links-by-sets` and `links-by-types`, exact contracts, linked conformance cases, and witness proofs. |
| R3 | Give Links Theory the simplest direct definition in itself. | Complete | `links-by-links`, addressed doublet template, and source/target/address rules. |
| R4 | Introduce set theory in multiple ways. | Complete | `universal.lino` supplies an extensional membership program and reusable constructor-relative program; both runtimes also expose canonical ordered-unique doublet-tree stores. |
| R5 | Represent strict finite sets as ordered sequences without duplicates. | Complete | `set-theory` linked rules and `encodeSet` / `encode_set` provide canonical insertion, equality, union, intersection, and image behavior. |
| R6 | Implement finite sequences through doublet links. | Complete | `LinkNetwork` and its Rust counterpart implement balanced, left, and right nested-tree encoders/decoders with mirrored tests. |
| R7 | Support direct and indirect self-referential sequences safely. | Complete | Explicitly bounded right-spine `walk`; mirrored direct/two-node cycle tests. |
| R8 | Unify concepts, local terms, and addresses. | Complete | Shared `(term theory local-name address)` links and address-mediated translation tests. |
| R9 | Make graphs a derived subset of links networks. | Complete | Linked vertex/endpoint/reachability/typing rules and separate `LinkGraph` convenience store. |
| R10 | Add relational algebra over links, sets, and types. | Complete | The `relational-algebra` program implements closure, converse, union, intersection, composition, and pair-typing inference. |
| R11 | Use `meta-language` for representation. | Partial — lossless text envelope only | `TheoryNetwork.fromRml` / `from_rml` round-trip candidate and foundation source, but `rml-meta-language.mjs` reconstructs text before LiNo parsing; the missing structured RML semantic representation is tracked by R101-R103. |
| R12 | Keep JS and Rust behavior aligned. | Complete | Shared `lib/meta-theory/*.lino` sources and mirrored `linked-program` / `theory-network` suites cover both runtimes. |
| R13 | Track the latest upstream meta-theory corpus exactly. | Complete | `upstream-0.0.3.lino`, its independent fingerprint/contract, declaration/dependency checks, and `formal-corpus.yml` pin revision `087f451`. |
| R14 | Do not delegate RML reasoning to Lean or Rocq. | Complete | `LinkedProgramRegistry` performs native linked reduction/inference; theory verification does not call the external corpus or a proof-assistant kernel. External builds are provenance/parity checks only. |
| R15 | Eliminate per-theory host adapters and branches. | Complete | `TheoryNetwork` executes every contract with one generic reduction/proof path. Source scans and tests cover previously unknown program names. |
| R16 | Define binding, substitution, and beta reduction through links. | Complete | De Bruijn lambda program in `universal.lino`; `((λx.x) a) -> a` and nested capture-safety tests in both runtimes. |
| R17 | Let users define a new logic without changing host source. | Complete | `theory-network-linked` tests define double-negation and modus-ponens programs plus verifier-level custom contracts with no callbacks. |
| R18 | Supply a universal constructor/experimenter for formal systems. | Partial — finite linked protocol delivered | `LinkedProgramRegistry` supports rebound imports, rewrites, facts, inference, proof traces, cycle checks, and bounds; `not a full language elaborator or arbitrary-foundation environment` remains the explicit gap. |
| R19 | Present familiar theories and their links-derived constructions. | Complete | The theory table and source walkthrough in `docs/META_THEORY.md`; conformance fixtures cover both reduction and judgement views. |
| R20 | Minimize and identify axioms. | Complete for this network | `foundation.lino` isolates named capability assumptions and proof premises; object behavior lives in linked rules, foundations remain separately selected, and candidates cannot author their own trust basis. |
| R21 | Update documentation and preserve a complete review trail. | Partial — latest request registered | `requirements.md` now registers reviews through the ordered-address follow-up `5799100000`; repository-wide behavior documentation and final evidence remain incomplete until R111, R121, R132, and R135 are delivered. |
| R22 | Use current dependency releases and prepare the next release. | Complete | `js/package.json` and `rust/Cargo.toml` align at 0.34.0; manifests and lockfiles prepare the shared-address-composition audit release, while maintained dependencies and Actions remain at the releases audited on 2026-09-21. |
| R23 | Make the meta-foundation explicit and inspectable. | Complete | The documented `S/K -> closed linked terms -> K1 -> F -> T` model and mirrored kernel reports separate two semantic contractions from parsing and resource control, with zero derived host services and zero object semantics. |
| R24 | Define meta-semantics as links above the bootstrap boundary. | Complete | `links-meta-foundation` defines object-encoded environment lookup, matching, substitution, rule selection/application, and result verification; mirrored tests execute an encoded copy of its own repeated-variable matching rule and compare it with direct execution. |
| R25 | Instantiate one unchanged theory over replaceable foundations. | Complete | Import-level `rebind` works across rewrites, facts, inferences, and transitive imports. One portable classifier returns `reject` or `accept` under strict/permissive user foundations without changing its source. |
| R26 | Reuse one set-theory definition in traditional and associative contexts. | Complete | `set-theory-over-traditional-sequences` and `set-theory-over-associative-links` rebind constructor concepts while importing the same complete set program; mirrored membership tests execute both. |
| R27 | Minimize K0 through an explicit experimental loop. | Complete for the current implementation | `bootstrapMetricsReport` / `bootstrap_metrics_report` fault-inject S, K, parsing, and bounds. S and K are necessary for the current representation/probe; parsing and bounds remain separately non-semantic. |
| R28 | Add a stronger self-interpretation witness. | Complete | K1 interprets an encoded copy of its own non-linear `match-identical-atoms` pattern, including repeated-variable equality and substitution; JS/Rust require agreement with direct K0 execution and inspect the K1 trace. |
| R29 | Publish a machine-readable K0 dependency/trust graph. | Complete | Both reports expose `rml-bootstrap-trust-graph/v1`, including S/K, six links-defined services, non-semantic boundary layers, and public semantic paths with explicit dependencies. |
| R30 | Fail CI when host semantics are absent from the trust graph. | Complete | Mirrored `auditBootstrapKernel` / `audit_bootstrap_kernel` tests compare an independent implementation manifest with the report and reject a simulated `hidden-object-evaluator`. The audit also requires every dependency branch to terminate in K0. |
| R31 | Do not call the current K0 irreducible without proof. | Complete | Source and documentation consistently use “current bootstrap boundary,” expose `claimsIrreducible: false`, and state the criterion for any later removal. |
| R32 | Give a structural reason for each primitive that remains. | Complete | Every bootstrap node contains a non-empty `primitiveReason` / `primitive_reason`; mirrored tests make missing justifications fail. |
| R33 | Count the complete host semantic surface, not only K0 labels. | Complete | `bootstrapMetricsReport` / `bootstrap_metrics_report` executes a common probe and reports exactly two host semantic operations: S and K contraction. Parsing and bounds are visible but excluded from the semantic count. |
| R34 | Classify primitive-removal experiments without hiding equivalent re-encodings. | Complete for measured candidates | Every boundary operation can be disabled at its execution point. S and K are `INDEPENDENT` relative to the current representation/probe. An executable iota witness preserves identity, discard, and duplication, but records `surfaceLawCount: 1`, `residualExternalSemanticLawCount: 2`, and `semanticInformationReduced: false`; the report explicitly rejects a global irreducibility claim. |
| R35 | Count host/linked semantic duplication. | Complete | `hostLinkedDuplications` / `host_linked_duplications` are empty because matching, substitution, traversal, import/rebinding, inference, and verification have only closed-term implementations. |
| R36 | Quantify self-hosting closure. | Complete for the declared probe | The linked-load/import/reduce/infer/self-verify task uses six links-defined semantic capabilities and zero host-defined counterparts, so closure is `6/6`, with every capability named. |
| R37 | Separate host information by layer. | Complete | `hostSemanticLayers` / `host_semantic_layers` separate semantic bootstrap (2), derived semantics (0), parsing (1), resource control (1), observability (0), and object-specific semantics (0). |
| R38 | Compare runtime-observed semantics with the trust graph. | Complete | `runtimeTrustGraphCoverage` / `runtime_trust_graph_coverage` record four paths and 10 segments; CI requires complete reachability and zero undocumented paths, operations, or segments. |
| R39 | Publish a foundation compression experiment. | Complete for measured candidates | The executable semantic host surface falls from eight operations to the S/K basis, so the current ratio is `2/8`. The report scopes necessity to its representation/probe and does not extrapolate to global minimality. |
| R40 | Publish previous/current metric deltas on every foundational iteration. | Complete | The versioned `rml-bootstrap-metrics/v4` comparison records known values from `8b39df5`, uses `null` for previously unmeasured values, and is printed in the bootstrap workflow. |
| R41 | Remove the large host-language semantic description from the runtime. | Complete | The authoritative 1,446-node program is `fixed-point-source.lino`; the runtime no longer contains `buildSourceKernel`, and generator-consistency tests compile all 25 roots to the checked-in artifact. |
| R42 | Anchor the bootstrap representation in upstream Links Theory. | Complete for representation | The source and artifact are addressed doublet networks tied to the upstream `network-duplet-function` model. The audit explicitly notes that upstream defines structure and self-definition, not an execution transition. |
| R43 | Classify the provenance of every residual semantic contribution. | Complete | Mirrored reports distinguish `represented-as-addressed-links`, `derived-inside-system`, `compiled-from-external-semantic-description`, and `externally-primitive`; S/K are explicitly external laws and the six higher capabilities are derived. The representation label does not claim ontological provenance. |
| R44 | Test the current host representation with its semantic operations disabled. | Complete for the declared probe | `foundationSearchExperiments` / `foundation_search_experiments` record that disabling S and K together fails the complete probe. This is evidence about the current passive host model, not link ontology. S/K succeeds; the iota experiment executes separately, observes both operations, and reports a surface re-encoding rather than reduced semantic information. |
| R45 | Make proof evidence establish its exact role, premises, substitution, dependencies, and conclusion. | Complete | `core.lino` encodes proof objects; `rml-theory-network.mjs` and `theory_network.rs` replay them against exact expected judgements. Mirrored `theory-network` and `proof-substrate` tests reject missing witnesses, unrelated valid proofs, damaged premises, unresolved dependencies, and proof cycles. |
| R46 | Keep the trusted foundation independent from the candidate being verified. | Complete | `foundation.lino` and `upstream-0.0.3-foundation.lino` are separately loaded contracts. Mirrored `theory-network` regressions reject candidate-supplied rules, axioms, adapter contracts, conformance cases, assumptions, and expected obligations. |
| R47 | Preserve and semantically account for the complete finite upstream 0.0.3 formal content, not only declaration names. | Partial — source preservation complete, semantic stages open | `upstream-0.0.3.lino` preserves 229 declarations, 8,815 typed tokens, ranges, recursion flags, and 510 dependency links; `semantic elaboration, native RML execution, and proof replay per declaration are not yet supplied` (R121-R124). |
| R48 | Perform actual RML reasoning without Lean/Rocq or theory-specific host authorization. | Complete | `LinkedProgramRegistry` executes linked rewrites, facts, imports, and inference uniformly in both runtimes; `TheoryNetwork` verifies contracts through that path. The `formal-corpus` native builds are parity/provenance checks and cannot authorize an RML witness. |
| R49 | Let users describe and execute arbitrary new formal systems relative to selected foundations. | Partial — unknown finite programs work | `universal.lino` and mirrored unknown-program tests execute double-negation and modus ponens with unseen names; `concurrent versioned foundations, linked rule packages, guarded coinduction, and linked probability policies remain open` (R115-R120). |
| R50 | Use “links network” for the ambient structure and reserve “graph” for the derived subset. | Complete | Repository guidance in `README.md`, `ARCHITECTURE.md`, and `docs/META_THEORY.md` uses the distinction; `TheoryNetwork` is ambient and the linked graph program plus `LinkGraph` require vertices and typed endpoints in mirrored tests. |
| R51 | Refresh maintained dependencies and report missing dependency capabilities when encountered. | Blocked by upstream — npm/Rust streams diverge | Rust uses `meta-language 0.58.2` while npm exposes only `0.46.0`; upstream issue `link-foundation/meta-language#195` records missing parser/translation delivery and must be consumed and tested before completion. |
| R52 | Align repository-wide architecture and vision documentation with the latest links-represented design. | Partial — current bootstrap documented | `README.md`, `ARCHITECTURE.md`, `docs/FOUNDATIONS.md`, and `docs/META_THEORY.md` align on the current `external laws -> linked source -> K1 -> F -> T` boundary; `full-language, recursive ontology, workflow, and open-gap documentation is not yet repository-wide` (R132). |
| R53 | Import one unchanged theory while redefining any compatible term or concept for its target foundation. | Complete | Generic `(rebind from to)` support in `rml-linked-program.mjs` and `linked_program.rs` applies transitively to rewrites, facts, inference rules, and imports; mirrored tests cover strict/permissive foundations and constructor rebinding. |
| R54 | Support both traditional and associative definitions while reducing independent axioms to the measured minimum. | Complete for the implemented foundations | `universal.lino` instantiates one set program over `traditional-sequence-foundation` and `associative-links-foundation`; linked programs similarly define types, graphs, and relations. `foundation.lino` isolates assumptions, while bootstrap metrics count the remaining external semantics instead of claiming axiom-free execution. |
| R55 | Reimplement and execute the RML semantic path from LiNo/link source with JavaScript/Rust parity. | Partial — declared bootstrap path only | `fixed-point-source.lino` and mirrored suites execute load, rebind, reduce, infer, and self-verify; `the real language/foundation operations requested in R99-R120 are outside that closure proof`. |
| R56 | Track every requirement-bearing issue/PR source and prevent silent ledger regressions. | Partial — cutoff exhaustive, continuing scan required | The register covers issue/PR requirements through `5799100000`; `issue-183-requirements.test.mjs` enforces sources, contiguous IDs, statuses, and evidence, while `a fresh paginated final scan is still required` by R97. |
| R57 | Search across genuinely different meta-foundation architectures instead of following only the lambda-to-S/K path. | Complete for executable controls, not a comparison cohort | `rml-foundation-search.mjs` executes closed S/K, the independent pre-S/K direct structural interpreter, and monotone Horn saturation; trace tests prove the latter two never observe S/K, while `comparisonEligibility` prevents treating them as reduced peers. |
| R58 | Record a falsifiable architecture for every candidate. | Complete | `rml-alternative-foundation-search/v10` and `foundation-candidates.json` record representation, laws, transition authority, provenance, host/formation/control boundaries, self-description, self-interpretation, self-generation, derived information, object-specific host knowledge, comparison eligibility, and the pre-semantic model assumptions. |
| R59 | Run one common load/import/rebind/match/substitute/rewrite/infer/verify/self-interpret workload. | Complete | `foundation-search.test.mjs` and `foundation_search_tests.rs` require all nine operations under all three mechanisms; `alternative-foundations.lino` defines no S/K transition or bracket-abstraction machinery for the controls. |
| R60 | Compare candidates with neutral quantitative metrics and a machine-readable table. | Complete without asymmetric ranking | The v10 report and `foundation-candidates.json` publish raw external information, host operations/sources, duplication, closure, compression, trust coverage, object semantics, undocumented authority, and computed execution eligibility. `asymmetricRankingPermitted: false` prevents incomparable raw counts from selecting a winner. |
| R61 | Fault-inject every primitive, claim equivalence only with executable evidence, and make no global minimality claim. | Complete for measured controls | Mirrored tests disable all `13` residual operations and require failure of the complete workload; every row has `equivalentTo: null`, the report requires executable bisimulation, sets `globallyMinimal: false`, and returns no minimum from an insufficient cohort. |
| R62 | Represent substitution and semantic artefacts recursively as links above a precise external boundary. | Partial — selected bootstrap only | `fixed-point-source.lino` represents six bootstrap capabilities above two contractions; `terms, typing, foundations, complete language semantics, and all new operations are not recursively closed as links` (R110-R114, R130). |
| R63 | Reason about linked definitions, axioms/rules, dependent statements, theorem conclusions, and proofs. | Partial — executable subset only | `universal.lino` and `core.lino` replay exact linked witnesses for the bundled subset; `the 229-declaration corpus does not yet have native RML elaboration and replay` (R121-R124). |
| R64 | Demonstrate full self-hosting for the selected foundation and allow multiple re-definable relative foundations to coexist. | Partial — bootstrap closure, not product closure | Candidate A reports `9/9` closure and the registry loads named examples; `versioned linked foundation packages, comparable peers, and closure of language/foundation operations remain open` (R115-R120, R126, R130). |
| R65 | Supply a Turing-complete linked execution model with a proof argument and executable witness. | Complete for the idealized unbounded transition relation, not minimality | `link-register-machine` and Horn clauses implement increment and conditional decrement for both counters. Mirrored witnesses exercise all `6` transition cases and halt at zero/zero; `foundation-search.md` gives the step-simulation induction, identifies the external theorem, and explicitly prevents universality from authorizing a foundation ranking. |
| R66 | Implement full JavaScript, Rust, Rocq, and Lean representations and translations through RML without delegating authority. | Partial — demonstrations only | Current candidates execute JavaScript/Rust counter-machine examples and Lean/Rocq identity judgements; `full grammar, elaboration, runtime/proof semantics, and all 12 directed translation paths are missing` (R99-R109). |
| R67 | Exploit self-similar/referential links as an implementation technique and identify research value. | Complete for the guarded witness | `guarded-referential-links` and its Horn counterpart use repeated-address unification to emit a finite observation plus opaque continuation. Positive/negative parity tests prevent endpoint mismatch; the documented proof-knot technique preserves the separate rejection of circular proof justification. |
| R68 | Document the architecture-neutral experiment, proof obligations, limitations, and all new review requirements exhaustively. | Complete | `foundation-search.md`, the case-study index, architecture/runtime guides, this ledger, and `issue-183-requirements.test.mjs` cross-link the implementation, finite symmetry result, and enforced claim boundary. |
| R69 | Keep alternative-foundation execution and elimination evidence aligned in JavaScript, Rust, and CI. | Complete | `foundation-search.test.mjs`, `foundation_search_tests.rs`, shared `alternative-foundations.lino`, the synchronized JSON table, and the `bootstrap` report step independently exercise both runtimes, including the symmetry quotient, and publish the comparison. |
| R70 | Admit a candidate to foundation ranking only after comparable reduction and self-hosting. | Complete | `comparisonEligibility` is computed from full linked closure, zero host/self duplication, zero external semantic source descriptions, and complete trust coverage. Regression tests admit A, exclude B/C with exact reasons, require two peers, and forbid asymmetric ranking. |
| R71 | Determine which execution authority is intrinsic to links rather than merely executable over them. | Open — intrinsic authority unresolved | `rml-link-ontology-symmetry-experiment/v7` retains the `NEGATIVE_CONSTRAINT_ONLY` and `BASE_DERIVATION_CANNOT_CREATE_NEW_OCCURRENCE_DISTINCTIONS`, proves both reference-only and link-local information losses, and classifies only equality structure; the two-link incidence cycle is explicitly not interpreted as dynamics or execution. |
| R72 | Do not declare the alternative-foundation search complete or select S/K without a sufficient comparable cohort. | Complete as an enforced non-selection guard | The v10 report requires at least `2` execution-comparable candidates, reports `OPEN_NO_COMPARABLE_ALTERNATIVE`, returns no smallest candidate, sets `selectedFoundation: null`, and classifies A/B/C as `EXECUTABLE_CONTROLS_ONLY` for ontology. |
| R73 | Re-audit whether the experiment studies links or only a host representation, and do not assume its primitive categories are ontological facts. | Open — link ontology unresolved | The v10 report retains the ordered-host witness's four `ASSUMED_NOT_DERIVED` choices and marks its second observation `CONDITIONAL_REFINEMENT_PROBE_NOT_DERIVED`. The v7 audit removes single-link isolation while retaining and naming ordered link records, one reference slot, finite size, distinct addresses, and equality-only observation as contract-relative restrictions. |
| R74 | Keep link ontology, primitive categories, the structure/transformation relation, intrinsic semantic authority, and comparative minimality genuinely unresolved until independently derived. | Open — foundational investigation continues | `ontologySearch.status` remains `OPEN_INDEPENDENT_INVESTIGATION`; the finite experiment establishes `COMPLETE_INVARIANT_FOR_CONTRACT`, `NON_ABSOLUTE_FOR_SYMMETRIES`, and `CLASSIFIED_PER_ORDERED_REFERENCE_SLOT` while explicitly classifying occurrence permutation as `UNESTABLISHED_EQUIVALENCE`. Mirrored tests enforce those results without closing any of the five broader questions. |
| R75 | Audit the provenance of every proposed primitive without importing an expected target architecture. | Complete as an independence guard | `ontologySearch.importedPrimitiveCategories` marks data/operation/state/transition/interpreter/evaluator/rewrite/rule/function/relation as `IMPORTED_EXPERIMENTAL_VOCABULARY` with `UNESTABLISHED` foundational status, sets `targetArchitectureSelected: false`, and publishes three provenance questions plus the derivation acceptance criterion. |
| R76 | Keep A/B/C as controls that cannot constrain ontology and use representation-level terminology instead of “link-native.” | Complete as a terminology and role guard | The v10 report sets `existingCandidatesRole: EXECUTABLE_CONTROLS_ONLY`, `existingCandidatesConstrainSearch: false`, and per-candidate `constrainsOntologySearch: false`; source provenance is `represented-as-addressed-links`, and tests reject `link-native` / `links-native` in the executable report. |
| R77 | Keep issue 183 open while its foundational research questions remain open. | Complete as a PR-state correction | PR 184 uses `Advances #183` rather than an auto-closing directive, while the versioned report retains `foundationStatus: OPEN` and the ledger keeps R71, R73, and R74 open. |
| R78 | Produce a substantive, falsifiable foundational research result rather than only more audit metadata. | Complete for the finite observation contract, not link ontology | `linkOntologySymmetryExperiment` / `link_ontology_symmetry_report` exhaust all three surjective assignments on their used support, six group elements, ten action applications, four selectors, and four self-maps; three encodings and two reification countermodels derive the exact eliminations asserted by `foundation-search.test.mjs` and `foundation_search_tests.rs`. |
| R79 | Determine what information the fixed two-occurrence contract loses without assuming a richer ontology. | Complete for widths one through four | `observationBoundary.arityEnumeration` reports `BINARY_CONTRACT_NOT_EXHAUSTIVE`, retains only unlabelled occurrences and equality, exhausts `1/3/13/75` surjective assignments, and derives `1/2/3/5` multiplicity-spectrum classes; `generalizedCompleteInvariant` explicitly limits completeness to the exhaustively tested widths. |
| R80 | Distinguish intentional quotienting from restrictions that demonstrably erase information. | Complete for the tested boundary | `lossAudit` classifies reference names as `DERIVED_EQUIVALENCE_WITHIN_ADDRESS_EQUALITY_CONTRACT`, occurrence order as `UNESTABLISHED_EQUIVALENCE`, fixed width as `PROVEN_INFORMATION_LOSS`, the conditional relation as `PROVEN_NOT_RECOVERABLE`, direct self-reference as `PROVEN_INFORMATION_LOSS_FOR_ADDRESSABLE_LINKS`, cross-link incidence as `PROVEN_INFORMATION_LOSS_UNDER_LOCAL_PROJECTION`, and endpoint direction/dynamics as `NOT_OBSERVED_NOT_DISPROVED`. |
| R81 | Test candidate distinctions across independently constructed representations. | Complete for the finite conditional probe | `canonical-partition-pair`, `paired-equality-matrices`, and `intersection-multiplicity-table` independently recover the same `33` joint classes in JavaScript and Rust. |
| R82 | Derive additional distinctions rather than adding a familiar semantic category to the starting contract. | Complete for multiplicity and conditional asymmetry | The unchanged base vocabulary derives wider multiplicity spectra. The separate second equivalence is explicitly `CONDITIONAL_REFINEMENT_PROBE_NOT_DERIVED`; only after that premise is exposed do automorphisms derive singleton occurrence orbits. |
| R83 | Measure which apparent primitives disappear under a changed observation model. | Complete for the tested forgetful projection | All five reference-only width-four classes have `5–9` inequivalent joint refinements, so the second relation is not recoverable after projection. Endpoint direction and dynamics are classified as unobserved rather than absent. |
| R84 | Identify structural facts invariant across the independently constructed finite models. | Complete for the enumerated representations | `canonical-partition-pair`, `paired-equality-matrices`, and `intersection-multiplicity-table` agree that multiplicity spectra classify the base family and intersection structure classifies the conditional pairs; tests require exact class and fibre counts in both runtimes. |
| R85 | Treat automorphisms as a research lead without promoting them to execution semantics or assuming they are exhaustive transformations. | Complete as a claim guard | `singletonOrbitHistogram` reports `20/5/7/1` classes with `0/1/2/4` singleton orbits. Only five classes select exactly one occurrence orbit; `remainingBoundary` forbids turning any orbit into source/target meaning or dynamics. |
| R86 | Preserve a destination-independent investigation instead of enriching the contract toward S/K, rewriting, Horn logic, or another expected calculus. | Complete for this iteration | The `v7` experiment adds only shared-address equality required to observe indirect self-reference; it labels cross-link incidence as neither endpoint, dependency, transition, nor execution, while A/B/C remain non-constraining controls. |
| R87 | Verify live PR metadata does not auto-close the issue while foundational research remains open. | Complete as a live metadata correction | PR 184 ends with `Advances #183`; `assertIssue183RemainsOpen` rejects GitHub closing keywords, and the `tests` workflow now runs on `pull_request.edited` and checks PR 184's event body while R71, R73, and R74 remain open. |
| R88 | Count singleton occurrence orbits precisely and do not equate “contains a singleton” with selecting one unique role. | Complete for all 33 width-four joint classes | `singletonOrbitHistogram` derives `20` classes with zero, `5` with one, `7` with two, and `1` with four singleton orbits in independent JavaScript, Rust, and import-free experiments. |
| R89 | Do not promote the widths-one-through-four enumeration to an unbounded invariant theorem. | Complete as a finite-scope guard | `generalizedCompleteInvariant` says `for each exhaustively tested unlabelled width 1 through 4`; the executable `remainingBoundary`, generated candidate table, docs, and mirrored tests repeat that no unbounded theorem is established. |
| R90 | Experimentally separate asymmetry forced by the investigated base object from asymmetry supplied or enabled by the conditional observation. | Complete for the exhaustive width-four probe | `asymmetryProvenance` partitions all 33 classes into `7` `BASE_FORCED`, `5` `REFINEMENT_PRESENT_NOT_BASE_FORCED`, `1` `RELATIONAL_INTERACTION_ONLY`, and `20` `NO_SINGLETON_ORBIT`. Four base fibres admit both outcomes; only `[3,1]` forces a singleton across all refinements. The `[0,0,1,2]` countermodel has no singleton with refinement `[0,0,0,0]` but four interaction-only singleton orbits with `[0,1,0,2]`, although each relation alone has orbit sizes `[2,2]`. |
| R91 | Prevent a versioned “live metadata verified” claim from passing when the external PR body is false. | Complete as an event-backed CI guard | `issue-183-requirements.test.mjs` reads `GITHUB_EVENT_PATH` for PR 184 and validates the actual body; `.github/workflows/tests.yml` includes `edited`, so body edits rerun the guard without a code commit. Fixture tests reject `Fixes #183`, `Closes #183`, and `Resolved #183`. |
| R92 | Determine whether the interaction-only distinction is forced by the tested base rather than merely enabled by the conditional observation. | Complete as a no-go result for derivation from the tested base | `derivationBoundary` exhausts all `255` base/candidate pairs at widths `1..4`: `73` preserve every base symmetry, `182` break at least one, and none of the preserving candidates changes a base occurrence orbit. The general four-step argument proves this for every finite deterministic derivation commuting with occurrence relabelling. The prior interaction-only pattern changes from `[0,1,0,2]` to `[0,1,1,2]` under `[1,0,2,3]`, while its base `[0,0,1,2]` remains fixed. Thus its new distinctions require information not derived from that base. |
| R93 | Keep the foundational derivation link-first rather than importing set, category, or type theory as authority. | Complete for the v7 ontology experiment | `rml-link-ontology-symmetry-experiment/v7` uses only address/reference equality, renaming, incidence, and relabelling. Direct and indirect self-reference requirements justify comparing link and reference addresses; no set, category, type, evaluator, transition law, or graph semantics authorizes the result. |
| R94 | Audit the faithfulness of the starting reference-only representation using independently justified information from links themselves. | Complete for direct self-reference at every nonzero finite arity | The issue requires directly self-referential addressed links, making “does a reference equal this link's address?” independently testable before any evaluator or calculus. `startingRepresentationAudit` constructs the direct-self `[0,0,1]` and fresh-external `[0,1,2]` countermodels with the same `[1,1]` reference-only projection. Exhaustive quotients at widths `1..4` collapse `2/4/7/12` addressable classes to `1/2/3/5`; generally, every nonempty multiplicity class has one fresh-address lift plus one self-identifying lift per distinct part size. Thus `REFERENCE_ONLY_PROJECTION_NOT_FAITHFUL_FOR_SELF_REFERENCE`, without claiming that retained link identity is a complete ontology. |
| R95 | Audit which equivalences in the repaired addressable representation are derived from links and which are introduced by the observer. | Complete for the declared address/equality contract; slot identity remains open | `quotientAudit` exhausts widths `1..4`: address renaming leaves `2/5/15/52` ordered equality classes and is `DERIVED_EQUIVALENCE_WITHIN_ADDRESS_EQUALITY_CONTRACT`, while occurrence permutation further collapses `0/1/8/40` classes and is `UNESTABLISHED_EQUIVALENCE`. The `[0,0,1]` / `[0,1,0]` countermodel now has explicit masks `[true,false]` / `[false,true]`. Equality matrices plus those masks are complete before the latter quotient; afterward, `(referenceMultiplicitySpectrum, directSelfReferenceMultiplicity)` is `COMPLETE_INVARIANT_FOR_DECLARED_UNLABELLED_ADDRESS_EQUALITY_CONTRACT`. This derives a smaller descriptor only under the declared unlabelled premise and keeps intrinsic reference-slot identity `UNRESOLVED`. |
| R96 | Audit every remaining assumption of the repaired addressed representation and produce another falsifiable result. | Partial — single-link isolation removed | The `sharedAddressComposition` audit varies only single-link isolation: it enumerates `2/10/77/799` shared-address classes at one through four ordered one-reference links, proves that local descriptors collapse the multi-link rows to `2/4/8/16`, supplies the external-pair/two-cycle countermodel, and classifies retained assumptions. The broader matrix in R127 remains open. |
| R97 | Preserve all prior requirements, register the latest requirements, and repeat a paginated source scan at the final commit. | Partial — current cutoff recorded | The source register covers comments through `5799100000` at `2026-09-23T16:58:55Z`; a new `final paginated scan` remains required immediately before completion. |
| R98 | Record original wording, normalized obligation, implementation, positive and negative evidence, status, and remaining gap for every atomic requirement. | Partial — normalized ledger and gaps restored | Immutable source links preserve `original wording`, rows preserve normalized requirements/status/evidence/gaps, but a separately queryable per-field machine-readable register is not yet complete. |
| R99 | Restore full-language scope and reassess the previously overstated R11/R18/R21/R47/R49/R51/R52/R55/R56/R62-R66 and live-state claims. | Complete for status correction | Rows `R11/R18/R21/R47/R49/R51/R52/R55/R56/R62-R64/R66` now state partial or blocked scope; R65 remains complete only for its idealized unbounded model, while R77/R87 describe the corrected live body. |
| R100 | Support truthful incomplete statuses while keeping traceability CI separate from a final completion gate. | Complete | `issue-183-requirements.mjs` accepts Complete/Partial/Open/Blocked statuses; `check-issue-183-completion.mjs` fails on every non-complete row and `issue-183-requirements.test.mjs` verifies that separation. |
| R101 | Inventory full JavaScript, Rust, Rocq, and Lean coverage by grammar, structure, scope, types, modules, recursion, proof forms, runtime facilities, standards, and extensions. | Open | Existing token CSTs and `foundation-search.md` explicitly exclude production coverage; no versioned `language coverage inventory` exists yet. |
| R102 | Reuse upstream meta-language infrastructure and integrate the implementation/release resolving upstream issue 195. | Blocked by upstream — issue 195 unreleased | Rust consumes `meta-language 0.58.2`, npm exposes `0.46.0`, and `link-foundation/meta-language#195` remains an external integration dependency; any interim extension must retain the upstream public contract. |
| R103 | Keep structured RML declarations, rules, assumptions, theories, proofs, substitutions, and references in a registered RML meta-language extension. | Open | `parseRmlToMetaLanguage` currently supplies a lossless source network and then reparses reconstructed text; a `structured RML semantic extension` is not implemented. |
| R104 | Round-trip full source from structured networks after discarding the source buffer and distinguish preservation, parsing, resolution, elaboration, execution, and verification. | Open | Existing CST/meta-language tests establish `source preservation` only; buffer-independent semantic reconstruction and the other named stages are absent. |
| R105 | Correct identifier rewriting for strings/comments, shadowing, capture, Unicode, and locations, with the same semantic/diagnostic corpus in both runtimes. | Partial | The JavaScript adapter now rewrites only `lino-cst.js.ident` tokens, preserves strings/comments, accepts Unicode identifiers, and reports UTF-16 offsets plus one-based lines/columns; mirrored regression coverage confirms the original case in both runtimes. Scope/shadowing/capture safety, identifiers inside template substitutions, escaped identifiers, and a shared comprehensive corpus remain open. |
| R106 | Implement all 12 directed JavaScript/Rust/Rocq/Lean translation paths plus structured RML import/export through the shared network. | Open | `translateTerm` performs shared-name lookup and the meta-language smoke renders a fixed string; `12 directed semantic paths` are absent. |
| R107 | Declare what each translation preserves across behavior, effects, ownership, types, universes, assumptions, and proof obligations. | Open | No versioned `translation preservation contract` exists for the four-language matrix. |
| R108 | Preserve unsupported constructs or return precise unsupported/unproved obligations without erasure, invented proofs, or relabelled source. | Open | No common `unsupported translation obligation` data model is implemented. |
| R109 | Test source-network-target-network preservation, native validity, and behavioral/proof equivalence on nontrivial positive and negative examples. | Open | Existing `translation smoke` and language-core demonstrations do not perform the required target builds or equivalence checks. |
| R110 | Implement the default linked `(Type -> Type)`, `(Type -> SubType)`, `(SubType -> Value)` ontology and explain orientation and productive recursion. | Complete | `recursive-type-ontology.md` specifies classifier-to-term orientation and the finite productive cycles; `withDefaultOntology`/`with_default_ontology` builds all three address-preserving links and their linked type facts in both runtimes. |
| R111 | Recursively close terms, addresses, types, values, rules, substitutions, judgements, proofs, foundations, and the ontology itself, with closure validation and identity/cycle-preserving serialization. | Partial | `validateClosure`/`validate_closure` now checks link/type-fact closure and the default ontology is closed. Whole-surface rule/proof/foundation closure, textual Pair-type definitions, lossless cyclic serialization, and the physical encoding boundary remain open. |
| R112 | Move authoritative typing relations from the TypedLinkNetwork side table into linked data and prove cache deletion/rebuild invariance. | Complete | `TypedLinkNetwork` now stores each declaration in an addressed type-fact `LinkNetwork`; semantic snapshots exclude the disposable index, and mirrored tests clear/query/rebuild the index without changing facts, typing results, or snapshots. |
| R113 | Map the default ontology explicitly to link-cli pinned types and Unicode orientation with interoperability tests. | Complete | `recursive-type-ontology.md` and the mirrored `linkCliInteropProfile`/`link_cli_interop_profile` tests pin `e801cb8`, prove the first two shapes exact and `Value` non-exact, map Unicode to subject/type facts, and reject implicit numeric name identity. |
| R114 | Keep the recursive ontology selectable and prevent its graph self-link from imposing Type : Type on every logical foundation. | Partial | The default is available only through an explicit factory; bare typed networks contain no `Type` link and proof/evaluation paths do not consume it automatically. Foundation-declared universe and soundness contracts are still open. |
| R115 | Load concurrent named/versioned linked foundation packages and carry selected foundation and assumptions with every result. | Open | The registry loads named programs, but lacks a `versioned foundation package/result context` contract separating truth, typing, equality, reduction, and proof rules. |
| R116 | Execute user-defined foundations as inspectable data through public APIs without adding host enum or switch cases. | Partial — generic finite rules exist | `LinkedProgramRegistry` executes previously unseen rewrite/inference names, but public theory verification still relies on fixed contract shapes and does not cover the full `foundation package` surface. |
| R117 | Demonstrate meaningfully different foundations and at least one unseen links-only logic through both public runtimes. | Partial — small classifiers only | Strict/permissive classifiers and unknown modus ponens run in both runtimes; `classical/intuitionistic or paraconsistent/resource/probabilistic foundation behavior` is not yet demonstrated through the complete public path. |
| R118 | Isolate assumptions between foundations and make inductive-cycle versus guarded-coinductive policy foundation-defined. | Open | Current cycle rejection is global for proof evidence; `foundation-scoped contradiction isolation and guarded coinduction` are missing. |
| R119 | Represent statement probabilities, confidence dependencies, and combination policies as links with dependency invalidation. | Partial — legacy probability theories exist | `lib/probabilistic` and evaluator events preserve probabilities, but `foundation-selected linked policies and dependency recalculation` are not delivered. |
| R120 | Report unknown, bounded exhaustion, unsupported, assumptions, and falsity distinctly without promising a universal decision procedure. | Partial — some bounds exist | Rewrite/inference APIs expose resource bounds, but a common `result status algebra` across languages/foundations is absent. |
| R121 | Map all 229 upstream declarations and non-declaration commands across preservation, elaboration, native RML execution/checking, dependencies, and admission status. | Partial — preservation column only | `FormalCorpus` and `upstream-0.0.3.lino` preserve 229 declarations at `087f451`; per-declaration `elaboration/execution/checking` and command coverage remain open. |
| R122 | Provide independently replayable RML definitions/proofs and reject body, binder, premise, proof, conclusion, and dependency mutations for the required meta-theory. | Open | Current corpus mutation checks cover source contracts; `native RML proof replay for the complete corpus` is absent. |
| R123 | Apply native RML execution and proof verification to complete set, type, Links, graph, and relational-algebra source specifications. | Partial — executable fragments exist | `universal.lino` exercises representative operations, but `complete named-theory content and proofs` are not encoded. |
| R124 | Keep the four upstream Lean sorry obligations explicitly admitted unless proofs are supplied. | Complete as an admission guard | `FormalCorpus` and case-study documentation report exactly `four admitted Lean proofs`; native builds and hashes are not presented as proofs. |
| R125 | Deliver a reproducible researcher workflow covering foundation definition, theories, theorem/proof inspection, execution, invalidation, comparison, and language import/export. | Open | Examples cover isolated operations; no single `end-to-end researcher workflow` exercises the requested lifecycle. |
| R126 | Commit a formal-ai adoption/rejection map for whole-source projection, typed events, context separation, and dependency recalculation. | Open | No `formal-ai efb6ddb experience map` with implementation tests is committed. |
| R127 | Explicitly enumerate repaired-representation assumptions, their forcing facts, and distinctions lost when each is removed, then add a falsifiable result. | Open | `quotientAudit` distinguishes two equivalences and `sharedAddressComposition` removes single-link isolation with a countermodel. The complete assumption-by-assumption removal matrix is still missing. |
| R128 | Preserve derived address-renaming versus unestablished occurrence permutation and retain self-reference, identity, arity, and possible slot identity. | Complete as a claim guard | `quotientAudit`, `slotwiseSelfIncidence`, `sharedAddressComposition`, mirrored tests, and the v7 report retain the renaming/order boundary, preserve direct and indirect incidence, and leave intrinsic slot and link-record order unresolved. |
| R129 | Treat the link-cli type ontology as a selectable construction rather than intrinsic foundational authority. | Open | The link-cli mapping itself is not implemented; documentation must classify it as a `candidate/default`, not ontology evidence. |
| R130 | Run comparable reduction, self-interpretation, and removal experiments for genuinely different candidate foundations before ranking. | Open — insufficient comparison cohort | `OPEN_NO_COMPARABLE_ALTERNATIVE` remains the correct v10 result because direct/Horn controls duplicate host semantics and are not reduced/self-hosted peers. |
| R131 | Keep idealized Turing-completeness evidence separate from finite controls, language completeness, ontology, and minimality. | Complete as a scope guard | R65 and `foundation-search.md` scope the two-counter simulation to an unbounded relation; `R71/R73/R74` and R130 remain open. |
| R132 | Extend source/artifact consistency, self-description, interpretation, and generation to real language and foundation operations while listing every residual host law. | Partial — six-operation probe only | `fixed-point-source.lino` closes the current six-operation bootstrap probe; `whole-product closure` for R101-R126 is not established. |
| R133 | Audit latest dependencies and consume required capabilities, explicitly tracking npm delivery and upstream issue 195. | Blocked by upstream — npm package unavailable | `js/package.json` remains correctly pinned to published 0.46.0 rather than naming a nonexistent version; integration cannot complete until `meta-language#195` is released and consumed. |
| R134 | Align all guides, examples, case-study data, capability data, and release notes with actual behavior and open gaps. | Partial — ledger corrected first | This `requirements.md` no longer claims broad completion, but the rest of the repository still needs an `open-gap documentation sweep`. |
| R135 | Run full runtime, parity, corpus, generation, lint/format/build, native-language, researcher-workflow, and translation checks with exact versions and results. | Open — full matrix not yet available | Implementation commit `559cf62` passes the implemented JavaScript, Rust, bootstrap, corpus, generation, parity, formatting, lint, native Lean/Rocq, documentation, and container checks, including the new slotwise scenarios; runs `35880095340` through `35881160126` record the exact versions and results. The full translation matrix and end-to-end researcher workflow remain unavailable until R101-R126 are delivered, so this is not final verification. |
| R136 | Remove every issue-closing directive while any requirement is open and verify the API body plus fresh CI without weakening guards. | Complete | `issue-183-pr-body.mjs` and the PR-184-only edited-event step in `tests.yml` remove externally restored closing directives through the GitHub API before testing the corrected local event payload; `issue-183-requirements.test.mjs` tests the repair and retains the strict live-event rejection. The API body ends with `Advances #183`; synchronize run `35852441345` passed on repair head `860ab5d`, and edited-event integration run `35853137658` passed after deliberately restoring `Fixes #183`. |
| R137 | Enumerate every requirement ID and evidence in the final response, list remaining gaps, and do not claim completion or close issue 183 while any gate is open. | Open — final handoff not reached | The separate `check:issue-183-final` command intentionally fails on this and every other incomplete row; final reporting must preserve the gaps. |
| R138 | Classify self-incidence per reference slot. | Complete for the ordered address/equality contract | `slotwiseSelfIncidence` / `slotwise_self_incidence` reports the exact Boolean mask for each ordered reference slot, realizes all `2/4/8/16` masks at widths `1..4`, and records `RENAMING_INVARIANT_PERMUTATION_EQUIVARIANT`. It combines with the reference-equality matrix into a complete finite descriptor. The result does not assert intrinsic slot identity or assign source, target, or execution meaning. |
| R139 | Continue from the ordered addressed object, vary assumptions individually, separate contract-forced, representation-stable, observer-added, and post-hoc semantic structure, and produce another falsifiable result without selecting a destination. | Complete for removal of single-link isolation; wider audit remains R127 | Mirrored `sharedAddressComposition` / `shared_address_composition` audits report `LOCAL_SINGLE_LINK_DESCRIPTOR_NOT_COMPOSITIONALLY_FAITHFUL` and retain all named restrictions except single-link isolation. At two links, `[[0,1],[2,3]]` and `[[0,2],[2,0]]` share both local single-link descriptors but differ globally: only the latter has a length-two address-incidence cycle. Exhaustive one-reference counts are `2/10/77/799` shared classes versus `2/4/8/16` local products for one through four ordered links. Cross-reference equality plus reference-to-link-address incidence is a complete invariant for all tested shared classes and has a general finite argument. The audit machine-readably separates issue-forced shared equality, renaming-stable distinction, observer-retained order/arity/size, and explicitly unassigned semantics. |

## Scope and claim semantics

The request to support **all possible formal systems** is implemented as a
syntax-independent linked-program protocol: users may supply new finite
rewrite and inference systems, foundations, assumptions, and relative
semantics without adding a theory-specific runtime branch. No finite test can
prove correctness of every possible future theory or enumerate the infinite
deductive closure of a nontrivial theory. The claim is therefore tied to the
**finite executable acceptance scope** above: generic execution, explicit
bounds, mirrored runtimes, negative mutation tests, and the complete pinned
upstream corpus.

Likewise, “as capable as Lean/Rocq” does not make an external proof assistant
part of the RML trust path. The pinned builds establish source parity and
regression evidence; **Lean/Rocq cannot authorize RML execution**. The RML
acceptance cases must pass through its linked evaluator and exact witness
checks even when both native builds succeed.

The remaining semantic boundary is not hidden or called globally minimal. It
is **two externally primitive S and K contractions** over the addressed-link
representation, plus separately classified parsing and resource control. The
machine-readable report retains `claimsIrreducible: false`: zero-transition
fails the declared probe, S/K passes it, and the executable iota experiment is
an equivalent re-encoding that still observes both laws.

The [architecture-neutral search](./foundation-search.md) adds direct
structural and Horn-relational controls, applies the same nine-operation
workload, and fault-injects all 13 measured laws. Its machine-readable
comparison sets `globallyMinimal: false` and does not claim pairwise
equivalence without executable bisimulation. The v10 execution-comparison gate
also sets `asymmetricRankingPermitted: false`: B and C remain outside the
comparison cohort while their host duplicates semantic capabilities. With only A
eligible, `OPEN_NO_COMPARABLE_ALTERNATIVE` is the enforced result and no
foundation is selected.

The ontology investigation is separately and explicitly
`OPEN_INDEPENDENT_INVESTIGATION`. Its machine-readable status remains: link
ontology `UNRESOLVED`; primitive categories: UNRESOLVED; structure/
transformation relation `UNRESOLVED`; intrinsic semantic authority
`UNRESOLVED`; and comparative minimality `UNRESOLVED`. A/B/C have role
`EXECUTABLE_CONTROLS_ONLY` and cannot constrain that search. The addressed
semantic source is classified `represented-as-addressed-links`; this states
its representation and does not claim that its semantic categories were
derived from link ontology.

The independent `rml-link-ontology-symmetry-experiment/v7` result begins with
only two unlabelled reference occurrences and equality. Exhaustive quotienting
shows that their equality partition is
`COMPLETE_INVARIANT_FOR_CONTRACT`. The distinct-reference class has no
invariant singleton selector, so endpoint direction is `NOT_DERIVABLE`; a
reified incidence model and unreified occurrence model have the same
projection, so separate link identity is `REPRESENTATION_DEPENDENT`; and both
identity and swap commute with every symmetry, leaving only a
`NEGATIVE_CONSTRAINT_ONLY` on representation-independent authority. The
contract is not claimed to exhaust links, and none of these results supplies a
positive execution law.

The v7 observation-boundary follow-up leaves that binary result intact but
proves it is not exhaustive outside fixed width. With the same primitives,
widths one through four have `1/2/3/5` multiplicity-spectrum classes. A
separate second equivalence is labelled
`CONDITIONAL_REFINEMENT_PROBE_NOT_DERIVED`: its 225 labelled pairs quotient to
33 joint classes, and forgetting it leaves `5–9` refinements in every coarse
fibre. Their singleton-orbit histogram is `20/5/7/1` for `0/1/2/4` singleton
orbits. Provenance then separates `7` base-forced classes, `5` whose refinement
already has a singleton, `1` interaction-only class, and `20` symmetric
classes. Four base fibres admit countermodels with and without a singleton;
only `[3,1]` forces one across every refinement. The interaction-only witness
uses two relations with orbit sizes `[2,2]` whose conjunction has four
singleton orbits. This derives structural interaction without establishing
the second relation, a unique endpoint role, or dynamics. Completeness of the
multiplicity invariant remains explicitly bounded to tested widths `1..4`.

The forcedness follow-up then tests the interaction possibility itself rather
than adding a third observation. Across all `255` base/candidate pairs at
widths `1..4`, exactly `73` candidates preserve every relabelling that
preserves their base, and none changes the base occurrence orbits. This finite
enumeration instantiates a general result: if a deterministic derivation
commutes with relabelling, every base-preserving relabelling also preserves its
output. The interaction-only conditional pattern fails that criterion under
the explicit `[1,0,2,3]` relabelling. Its symmetry breaking is therefore
supplied by information outside the tested base. The result uses no set,
category, or type-theory axiom and does not claim that the tested base exhausts
links.

The starting-representation audit then tests the weak projection rather than
patching it. Issue 183 independently requires addressed links that can refer
to themselves, so equality between the link address and its reference
occurrences is available before choosing an evaluator, endpoint role, or
calculus. The direct-self pattern `[0,0,1]` and fresh-external pattern
`[0,1,2]` have the same reference-only `[1,1]` multiplicity projection but
cannot be related by global address renaming or reference-occurrence
permutation. Exhaustive widths `1..4` contain `2/4/7/12` addressable classes
but only `1/2/3/5` reference-only classes. More generally, every nonempty
reference spectrum has a fresh-address lift and an inequivalent lift for each
distinct multiplicity whose reference class is identified with the link.
The projection is therefore non-injective at every nonzero finite arity. This
establishes one exact failure of the starting abstraction; it does not make
link identity a complete ontology or provide semantics.

The quotient audit then asks which transformations of that repaired
addressable representation have actually been earned. A full equality matrix
is invariant and complete under global bijective address renaming, so raw
address names add no information within the declared address/equality
contract. Reference-occurrence permutation is different: it collapses
`0/1/8/40` ordered classes at widths `1..4`, and no premise yet establishes
that reference slots intrinsically lack identity. The smaller multiplicity
plus self-reference descriptor is therefore complete only for the explicitly
unlabelled contract. This is a contract-relative reduction, not a claim that
the addressable quotient exhausts links.

The slotwise follow-up retains the ordered representation long enough to
classify self-incidence directly. For an addressed pattern
`[address, reference₀, …]`, its mask records whether each reference equals the
link address. Every one of the `2/4/8/16` possible masks occurs at widths
`1..4`. Global address renaming leaves the mask fixed; reference-slot
permutation transports it equivariantly instead of leaving it invariant. Thus
the reference-equality matrix plus this mask is complete for the tested
ordered address/equality patterns. The `[0,0,1]` and `[0,1,0]` patterns make
the distinction concrete: both have one self-reference, but in different
slots. This does not establish intrinsic slot identity or turn either slot
into a source, target, or execution role.

The shared-address follow-up removes only single-link isolation. At one
through four ordered one-reference links it derives `2/10/77/799`
shared-address classes, while products of the local descriptor distinguish
only `2/4/8/16`. The external-reference pair `[[0,1],[2,3]]` and two-link
incidence cycle `[[0,2],[2,0]]` therefore falsify compositional faithfulness.
Cross-reference equality plus reference-to-link-address incidence is complete
for the tested shared contract. Ordered records, the finite count, and one
reference slot remain observer choices; incidence is not assigned endpoint,
dependency, transition, or execution meaning.

The host-representation witness supplies only the result
`ORDERED_LINK_REPRESENTATION_UNDERDETERMINES_TESTED_TRANSITIONS`: two host
functions over the same tagged ordered value produce distinct results while
preserving its shape and atom-renaming behavior. Passivity, endpoint ordering,
external transition, and the separation between structure and transformation
are premises of that experiment. They are not derived properties of links.
The report therefore records `intrinsicTransitionAuthority: UNRESOLVED` and
`structureTransformationSeparation: ASSUMED_BY_EXPERIMENT`. Turing
completeness and language-core coverage demonstrate expressive power only;
they cannot establish link ontology or bootstrap minimality.

Because R71, R73, and R74 remain open, PR 184 records `Advances #183` and does
not contain a directive that would close the issue on merge.

Turing completeness is scoped to the idealized unbounded two-counter
transition relation plus the standard universality theorem. Concrete runs
remain resource bounded. Likewise, the Lean, Rocq, Rust, and JavaScript
deliverables are actual executable **semantic cores**, not complete production
implementations of their toolchains. Turing completeness cannot by itself
establish source compatibility, library coverage, or proof-assistant soundness.
They are not complete production implementations.

## Reviewer acceptance test

The exact minimal behavior is represented without lambda-specific host code:

```text
(beta (lambda (bound zero)) (free a))
  -> (substitute (bound zero) (free a))
  -> (evaluate (bound zero) (bind (free a) empty-environment))
  -> (free a)
```

The trace is produced by rules named `beta-reduction`,
`substitute-bound-variable`, and `evaluate-bound-zero` from
`universal.lino`. JavaScript and Rust assert the same result.

## Trust and scope statement

The bootstrap semantic mechanism is exactly two externally primitive S and K
contractions. Closed terms compiled from the authoritative addressed-link
`lib/meta-theory/fixed-point-source.lino` into `fixed-point.ski` implement matching,
substitution, traversal, import/rebinding, inference, and result verification
for both runtimes. Parsing and cycles/resource bounds remain explicit
non-semantic boundary layers. The host assigns no built-in meaning to
`lambda`, `set`, `graph`, `relation`, `Pi`, or RML truth constructors.
`links-meta-foundation` reconstructs the main interpreter relations as
link-level data and rules and self-interprets a non-linear rule.

This is a reproducible current fixed point, not a proof of irreducibility. An
operation may leave K0 only when all public semantic paths still execute and
the replacement does not presuppose the same operation under another name.
The report publishes the structural reason and experiment outcome so a future
implementation can repeat the loop rather than inherit the conclusion.

The separate [bootstrap metrics report](./bootstrap-metrics.md) records the
actual current distance from self-hosting: two external semantic laws, zero
external semantic source descriptions, zero duplicated semantics, `6/6`
closure, and `2/8` compression. Its zero-transition failure,
representation-scoped S/K classifications, and executable iota equivalence
witness prevent experimental necessity or a smaller vocabulary from being
mistaken for a global minimality proof.

The pinned Lean/Rocq corpus remains useful evidence that RML can preserve and
query the upstream development. It is deliberately outside the authorization
path for linked programs. Native kernel success cannot substitute for a failed
RML conformance case or witness proof.
